<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name, label: false, placeholder: "What's the name of your cocktail?", class: "form-control" %>
  <%= f.submit class: "btn btn-primary form-control" %>
<% end %>
